%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @user.followings.each do |following| 
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('knwang@gmail.com'.downcase)}?s=40")
            = link_to following.subject.full_name, user_path(following.subject)
          %td.extra-padding #{following.subject.queue_items.size} 
          %td.extra-padding #{number_of_followers(following.subject)}  
          %td.extra-padding
            = link_to following_path(following), method: :delete do
              %i.glyphicon.glyphicon-remove
